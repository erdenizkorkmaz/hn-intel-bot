# Environment Variables

Copy these to your GitHub Secrets (Settings → Secrets and variables → Actions):

```
KEYWORDS=startup,SaaS,AI,machine learning,developer tools
MIN_SCORE=10
MAX_AGE_HOURS=24
WEBHOOK_URL=
```

**Note:** WEBHOOK_URL is optional. Leave empty if you don't need notifications.
